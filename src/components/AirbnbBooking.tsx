// This file will be replaced by the new FeaturedProperties section, as requested by the user.
import { motion } from 'framer-motion';
import FeaturedProperties from './FeaturedProperties';

const AirbnbBooking = () => {
  return (
    <FeaturedProperties />
  );
};

export default AirbnbBooking;
